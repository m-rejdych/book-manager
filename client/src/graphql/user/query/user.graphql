query User($id: ID) {
  user(id: $id) {
    id
    email
    firstName
    lastName
    fullName
    books {
      id
      title
      description
      author
      isRead
      category {
        id
        name
      }
    }
  }
}
